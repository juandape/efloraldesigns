import React from 'react';
import FlowerCarousel from './FlowerCarousel';

const Mothers: React.FC = () => {
  return (
    <FlowerCarousel
      occasion='mothers'
      title='Mothers'
      link='special-occasions/mothers'
    />
  );
};

export default Mothers;
